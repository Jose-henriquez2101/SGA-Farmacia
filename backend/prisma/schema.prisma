generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://admin:admin123@database-1.cps4ga2ia6kh.sa-east-1.rds.amazonaws.com:3306/crud_db"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}

model Cliente {
  id        Int    @id @default(autoincrement())
  nombre    String
  rut       String @unique
  direccion String
  telefono  String
}
